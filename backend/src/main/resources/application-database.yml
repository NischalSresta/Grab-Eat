#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/grabeat_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#    username: grabeat_user
#    password: 12345
#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#        jdbc:
#          batch_size: 20
#        order_inserts: true
#        order_updates: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grabeat_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: grabeat_user
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
        order_inserts: true
        order_updates: true